# 词条系统重构 实现计划

> **给 AI 助手：** 必需子技能：使用 superpowers:executing-plans 或 superpowers:subagent-driven-development 实现此计划。选择取决于执行环境偏好。

**目标：** 将词条系统从数据驱动重构为函数驱动，支持纯函数、状态列表、可序列化

**架构：** 
- 随从效果以纯函数形式定义，输入 `EffectContext` 返回新 `GameState`
- 能力值变化通过 `statusList` 管理，伤害直接扣 `currentHealth`
- 函数通过 `toString()` 序列化，`new Function()` 反序列化

**技术栈：** TypeScript, Zod, CloneDeep

**设计文档：** `docs/design/2026-03-11-词条系统重构-design.md`

---

## 任务 1：更新 Follower 类型定义

**文件：**
- 修改：`packages/shared/src/types/follower.ts`
- 修改：`packages/shared/src/types/index.ts`

**步骤 1：添加新的类型定义**

在 `packages/shared/src/types/follower.ts` 中添加：

```typescript
// 触发时机
export type TriggerTiming =
  | 'onEnter'       // 入场
  | 'onGrowth'      // 成长
  | 'onFirstStrike' // 先手
  | 'onAttack'      // 攻击后
  | 'onHit'         // 受击
  | 'onKill'        // 击杀
  | 'onDeath'       // 死亡（遗言）
  | 'onRoundEnd'    // 回合结束（准备）

// 状态项
export interface StatusItem {
  attack?: number      // 攻击力加成
  health?: number      // 生命上限加成
  shield?: number      // 护盾层数
  permanent: boolean   // 是否永久保留
  source: string       // 来源描述
}

// 效果上下文（前置声明，实现需要导入 GameState）
export interface EffectContext {
  gameState: unknown   // GameState，避免循环引用
  self: FollowerInstance
  tools: EffectTools
  event?: GameEvent
}

// 效果工具
export interface EffectTools {
  cloneDeep: <T>(obj: T) => T
  getRandomEnemy: (state: unknown, pos?: BattlePosition) => FollowerInstance | null
  getRandomAlly: (state: unknown, pos?: BattlePosition) => FollowerInstance | null
  getAllAllies: (state: unknown) => FollowerInstance[]
  getAllEnemies: (state: unknown) => FollowerInstance[]
  getColumnAllies: (state: unknown, x: number) => FollowerInstance[]
}

// 游戏事件
export interface GameEvent {
  type: 'attack' | 'damage' | 'kill' | 'death'
  source?: FollowerInstance
  target?: FollowerInstance
  value?: number
}

// 效果函数
export type EffectFunction = (ctx: EffectContext) => unknown

// 效果定义
export interface EffectDefinition {
  description: string
  execute: EffectFunction
}

// 序列化效果
export interface SerializedEffect {
  description: string
  execute: string
}
```

**步骤 2：更新 FollowerTemplate**

```typescript
// 随从模板
export interface Follower {
  id: string
  name: string
  description: string
  level: 1 | 2 | 3 | 4 | 5 | 6
  baseAttack: number
  baseHealth: number
  effects: Partial<Record<TriggerTiming, EffectDefinition>>
  equipmentSlots: number
  imageUrl: string
}

// 序列化随从
export interface SerializedFollower extends Omit<Follower, 'effects'> {
  effects: Partial<Record<TriggerTiming, SerializedEffect>>
}
```

**步骤 3：更新 FollowerInstance**

```typescript
// 随从实例（战场上）
export interface FollowerInstance extends Omit<Follower, 'effects'> {
  instanceId: string
  ownerId: string
  position: BattlePosition
  currentHealth: number
  statusList: StatusItem[]
  equipment: string[]
}
```

**步骤 4：更新 Zod 模式**

```typescript
// 状态项 Schema
export const statusItemSchema = z.object({
  attack: z.number().optional(),
  health: z.number().optional(),
  shield: z.number().optional(),
  permanent: z.boolean(),
  source: z.string()
})

// 序列化效果 Schema
export const serializedEffectSchema = z.object({
  description: z.string(),
  execute: z.string()
})

// 触发时机 Schema
export const triggerTimingSchema = z.enum([
  'onEnter', 'onGrowth', 'onFirstStrike', 'onAttack',
  'onHit', 'onKill', 'onDeath', 'onRoundEnd'
])

// 更新 followerSchema
export const followerSchema = z.object({
  id: z.string(),
  name: z.string(),
  description: z.string(),
  level: z.union([z.literal(1), z.literal(2), z.literal(3), z.literal(4), z.literal(5), z.literal(6)]),
  baseAttack: z.number(),
  baseHealth: z.number(),
  effects: z.record(z.any()).optional(), // 运行时验证
  equipmentSlots: z.number(),
  imageUrl: z.string()
})

// 更新 followerInstanceSchema
export const followerInstanceSchema = followerSchema.extend({
  instanceId: z.string(),
  ownerId: z.string(),
  position: battlePositionSchema,
  currentHealth: z.number(),
  statusList: z.array(statusItemSchema),
  equipment: z.array(z.string())
})
```

**步骤 5：导出类型**

在 `packages/shared/src/types/follower.ts` 末尾确保导出：

```typescript
export type {
  TriggerTiming,
  StatusItem,
  EffectContext,
  EffectTools,
  GameEvent,
  EffectFunction,
  EffectDefinition,
  SerializedEffect,
  Follower,
  FollowerInstance,
  SerializedFollower
}
```

**步骤 6：提交**

```bash
git add packages/shared/src/types/follower.ts
git commit -m "refactor(types): 更新 Follower 类型定义 - 支持函数驱动效果"
```

---

## 任务 2：精简 Keyword 类型定义

**文件：**
- 修改：`packages/shared/src/types/keyword.ts`

**步骤 1：移除旧的效果类型**

删除或注释掉旧的类型：
- `EffectType`
- `TargetType`  
- `ConditionType`
- `Condition`
- `EffectDefinition`（旧的）
- 对应的 Zod 模式

**步骤 2：保留必要的类型**

```typescript
// packages/shared/src/types/keyword.ts

// 此文件保留向后兼容，新系统使用 follower.ts 中的类型

// 旧的 Keyword 类型保留用于向后兼容
export interface Keyword {
  id: string
  name: string
  description: string
  trigger?: string
  effect: {
    type: string
    value?: number | string
    target: string
  }
  stacks?: boolean
  visible: boolean
}

// 旧的 Zod 模式保留用于向后兼容
import { z } from 'zod'

export const keywordSchema = z.object({
  id: z.string(),
  name: z.string(),
  description: z.string(),
  trigger: z.string().optional(),
  effect: z.object({
    type: z.string(),
    value: z.union([z.number(), z.string()]).optional(),
    target: z.string()
  }),
  stacks: z.boolean().optional(),
  visible: z.boolean()
})

export type KeywordType = z.infer<typeof keywordSchema>
```

**步骤 3：提交**

```bash
git add packages/shared/src/types/keyword.ts
git commit -m "refactor(types): 精简 Keyword 类型 - 保留向后兼容"
```

---

## 任务 3：添加状态计算工具函数

**文件：**
- 创建：`packages/shared/src/utils/follower.ts`
- 修改：`packages/shared/src/utils/index.ts`

**步骤 1：创建 follower.ts**

```typescript
// packages/shared/src/utils/follower.ts
import type { FollowerInstance, StatusItem } from '../types/follower'

/**
 * 计算随从当前属性
 */
export function getCurrentStats(follower: FollowerInstance) {
  const statusTotal = follower.statusList.reduce(
    (acc, s) => ({
      attack: acc.attack + (s.attack ?? 0),
      health: acc.health + (s.health ?? 0),
      shield: acc.shield + (s.shield ?? 0)
    }),
    { attack: 0, health: 0, shield: 0 }
  )

  return {
    attack: follower.baseAttack + statusTotal.attack,
    maxHealth: follower.baseHealth + statusTotal.health,
    shield: statusTotal.shield,
    currentHealth: follower.currentHealth
  }
}

/**
 * 清除临时状态
 */
export function clearTemporaryStatus(follower: FollowerInstance): FollowerInstance {
  return {
    ...follower,
    statusList: follower.statusList.filter(s => s.permanent)
  }
}

/**
 * 添加状态项
 */
export function addStatus(
  follower: FollowerInstance,
  status: Omit<StatusItem, 'source'> & { source: string }
): FollowerInstance {
  return {
    ...follower,
    statusList: [...follower.statusList, status as StatusItem]
  }
}

/**
 * 计算生命上限
 */
export function getMaxHealth(follower: FollowerInstance): number {
  return follower.baseHealth + 
    follower.statusList.reduce((sum, s) => sum + (s.health ?? 0), 0)
}

/**
 * 检查是否死亡
 */
export function isDead(follower: FollowerInstance): boolean {
  return follower.currentHealth <= 0
}
```

**步骤 2：更新 index.ts 导出**

在 `packages/shared/src/utils/index.ts` 添加：

```typescript
export * from './follower'
```

**步骤 3：提交**

```bash
git add packages/shared/src/utils/follower.ts packages/shared/src/utils/index.ts
git commit -m "feat(utils): 添加随从状态计算工具函数"
```

---

## 任务 4：添加序列化工具

**文件：**
- 创建：`packages/shared/src/utils/serialization.ts`
- 修改：`packages/shared/src/utils/index.ts`

**步骤 1：创建 serialization.ts**

```typescript
// packages/shared/src/utils/serialization.ts
import type {
  Follower,
  SerializedFollower,
  EffectDefinition,
  SerializedEffect,
  TriggerTiming,
  EffectFunction
} from '../types/follower'

/**
 * 序列化效果定义
 */
export function serializeEffect(effect: EffectDefinition): SerializedEffect {
  return {
    description: effect.description,
    execute: effect.execute.toString()
  }
}

/**
 * 反序列化效果定义
 */
export function deserializeEffect(serialized: SerializedEffect): EffectDefinition {
  return {
    description: serialized.description,
    execute: new Function('ctx', `return (${serialized.execute})(ctx)`) as EffectFunction
  }
}

/**
 * 序列化随从模板
 */
export function serializeFollower(follower: Follower): SerializedFollower {
  const serialized: SerializedFollower = {
    ...follower,
    effects: {}
  }
  
  for (const [trigger, effect] of Object.entries(follower.effects)) {
    if (effect) {
      serialized.effects[trigger as TriggerTiming] = serializeEffect(effect)
    }
  }
  
  return serialized
}

/**
 * 反序列化随从模板
 */
export function deserializeFollower(serialized: SerializedFollower): Follower {
  const follower: Follower = {
    ...serialized,
    effects: {}
  }
  
  for (const [trigger, effect] of Object.entries(serialized.effects)) {
    if (effect) {
      follower.effects[trigger as TriggerTiming] = deserializeEffect(effect)
    }
  }
  
  return follower
}
```

**步骤 2：更新 index.ts 导出**

在 `packages/shared/src/utils/index.ts` 添加：

```typescript
export * from './serialization'
```

**步骤 3：提交**

```bash
git add packages/shared/src/utils/serialization.ts packages/shared/src/utils/index.ts
git commit -m "feat(utils): 添加效果序列化/反序列化工具"
```

---

## 任务 5：重写随从数据（示例随从）

**文件：**
- 修改：`packages/shared/src/data/followers.ts`

**步骤 1：更新导入**

```typescript
import type { Follower, FollowerInstance, EffectContext, TriggerTiming } from '../types/follower'

export type { Follower, FollowerInstance } from '../types/follower'
```

**步骤 2：重写 1 星随从**

```typescript
export const FOLLOWERS: Record<string, Follower> = {
  // ===== 1星随从 =====

  'F001': {
    id: 'F001',
    name: '小狼',
    description: '基础攻击型',
    level: 1,
    baseAttack: 2,
    baseHealth: 1,
    effects: {},
    equipmentSlots: 2,
    imageUrl: '/assets/followers/f001.png'
  },
  
  'F002': {
    id: 'F002',
    name: '刺客学徒',
    description: '击杀后获得+1/+1',
    level: 1,
    baseAttack: 1,
    baseHealth: 2,
    effects: {
      onKill: {
        description: '击杀后永久获得+1/+1',
        execute: (ctx: EffectContext) => {
          const newState = ctx.tools.cloneDeep(ctx.gameState)
          const self = newState.allies?.find((f: FollowerInstance) => f.instanceId === ctx.self.instanceId)
          if (self) {
            self.statusList.push({
              attack: 1,
              health: 1,
              permanent: true,
              source: '击杀效果'
            })
          }
          return newState
        }
      }
    },
    equipmentSlots: 2,
    imageUrl: '/assets/followers/f002.png'
  },
  
  'F003': {
    id: 'F003',
    name: '火焰精灵',
    description: '入场时对随机敌人造成2点伤害',
    level: 1,
    baseAttack: 2,
    baseHealth: 1,
    effects: {
      onEnter: {
        description: '入场时对随机敌人造成2点伤害',
        execute: (ctx: EffectContext) => {
          const newState = ctx.tools.cloneDeep(ctx.gameState)
          const target = ctx.tools.getRandomEnemy(newState, ctx.self.position)
          if (target) {
            target.currentHealth -= 2
          }
          return newState
        }
      }
    },
    equipmentSlots: 2,
    imageUrl: '/assets/followers/f003.png'
  },
  
  'F004': {
    id: 'F004',
    name: '铁皮猪',
    description: '基础防御型',
    level: 1,
    baseAttack: 1,
    baseHealth: 3,
    effects: {},
    equipmentSlots: 2,
    imageUrl: '/assets/followers/f004.png'
  },
  
  'F005': {
    id: 'F005',
    name: '持盾新兵',
    description: '入场获得护盾',
    level: 1,
    baseAttack: 1,
    baseHealth: 2,
    effects: {
      onEnter: {
        description: '入场时获得1层护盾',
        execute: (ctx: EffectContext) => {
          const newState = ctx.tools.cloneDeep(ctx.gameState)
          const self = newState.allies?.find((f: FollowerInstance) => f.instanceId === ctx.self.instanceId)
          if (self) {
            self.statusList.push({
              shield: 1,
              permanent: true,
              source: '入场护盾'
            })
          }
          return newState
        }
      }
    },
    equipmentSlots: 2,
    imageUrl: '/assets/followers/f005.png'
  },
  
  'F006': {
    id: 'F006',
    name: '嘲讽石像',
    description: '嘲讽',
    level: 1,
    baseAttack: 0,
    baseHealth: 4,
    effects: {},
    equipmentSlots: 2,
    imageUrl: '/assets/followers/f006.png'
    // 嘲讽作为静态属性，在 FollowerInstance 中标记
  },
  
  'F007': {
    id: 'F007',
    name: '种子精灵',
    description: '成长+1/+1',
    level: 1,
    baseAttack: 1,
    baseHealth: 1,
    effects: {
      onGrowth: {
        description: '成长时永久获得+1/+1',
        execute: (ctx: EffectContext) => {
          const newState = ctx.tools.cloneDeep(ctx.gameState)
          const self = newState.allies?.find((f: FollowerInstance) => f.instanceId === ctx.self.instanceId)
          if (self) {
            self.statusList.push({
              attack: 1,
              health: 1,
              permanent: true,
              source: '成长效果'
            })
          }
          return newState
        }
      }
    },
    equipmentSlots: 2,
    imageUrl: '/assets/followers/f007.png'
  }
  // ... 其他随从按同样模式重写
}
```

**步骤 3：保留辅助函数**

```typescript
// 快捷获取随从
export function getFollower(id: string): Follower | undefined {
  return FOLLOWERS[id]
}

// 获取所有随从
export function getAllFollowers(): Follower[] {
  return Object.values(FOLLOWERS)
}

// 按星级获取随从
export function getFollowersByLevel(level: 1 | 2 | 3 | 4 | 5 | 6): Follower[] {
  return Object.values(FOLLOWERS).filter(f => f.level === level)
}
```

**步骤 4：提交**

```bash
git add packages/shared/src/data/followers.ts
git commit -m "refactor(data): 重写随从数据 - 函数驱动效果"
```

---

## 任务 6：更新 keywords.ts 为向后兼容层

**文件：**
- 修改：`packages/shared/src/data/keywords.ts`

**步骤 1：简化 keywords.ts**

```typescript
// packages/shared/src/data/keywords.ts
// 此文件保留用于向后兼容，新系统不再使用独立的 keywords 表

import type { Keyword } from '../types/keyword'

/**
 * @deprecated 新系统使用 Follower.effects 直接定义效果
 */
export const KEYWORDS: Record<string, Keyword> = {
  // 保留旧的词条定义用于向后兼容
  // 新随从不再使用此表
}

/**
 * @deprecated
 */
export function getKeyword(id: string): Keyword | undefined {
  return KEYWORDS[id]
}

/**
 * @deprecated
 */
export function getAllKeywords(): Keyword[] {
  return Object.values(KEYWORDS)
}

export * from '../types/keyword'
```

**步骤 2：提交**

```bash
git add packages/shared/src/data/keywords.ts
git commit -m "refactor(data): 简化 keywords.ts 为向后兼容层"
```

---

## 任务 7：更新类型导出

**文件：**
- 修改：`packages/shared/src/types/index.ts`

**步骤 1：确保导出新类型**

```typescript
// packages/shared/src/types/index.ts
export * from './api'
export * from './follower'
export * from './game'
export * from './keyword'
export * from './phaser'
```

**步骤 2：提交**

```bash
git add packages/shared/src/types/index.ts
git commit -m "refactor(types): 更新类型导出"
```

---

## 任务 8：构建并验证

**步骤 1：构建 shared 包**

```bash
cd packages/shared && bun run build
```

预期：构建成功，无类型错误

**步骤 2：运行测试（如有）**

```bash
cd packages/shared && bun test
```

预期：测试通过

**步骤 3：最终提交**

```bash
git add .
git commit -m "refactor: 词条系统重构完成 - 函数驱动架构"
```

---

## 文件变更摘要

| 文件 | 操作 |
|------|------|
| `packages/shared/src/types/follower.ts` | 修改 - 添加新类型 |
| `packages/shared/src/types/keyword.ts` | 修改 - 精简 |
| `packages/shared/src/utils/follower.ts` | 创建 - 状态计算 |
| `packages/shared/src/utils/serialization.ts` | 创建 - 序列化 |
| `packages/shared/src/utils/index.ts` | 修改 - 导出 |
| `packages/shared/src/data/followers.ts` | 修改 - 重写效果 |
| `packages/shared/src/data/keywords.ts` | 修改 - 简化 |
| `packages/shared/src/types/index.ts` | 修改 - 导出 |

---

## 注意事项

1. **GameState 结构**：当前 `gameState` 类型需要包含 `allies` 和 `enemies` 数组，如果当前结构不同，需要调整 `EffectTools` 实现

2. **EffectTools 实现**：工具函数的实现放在 `packages/server`，不在 `shared` 包中

3. **嘲讽等静态属性**：需要在 `FollowerInstance` 中添加 `taunt?: boolean` 等属性

4. **完整随从数据**：本计划只展示了部分随从的重写，实际需要重写所有随从
