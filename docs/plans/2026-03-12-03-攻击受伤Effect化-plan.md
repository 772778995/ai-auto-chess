# 攻击/受伤 Effect 化 实现计划

> **给 AI 助手：** 必需子技能：使用 executing-plans 或 subagent-driven-development 实现此计划。选择取决于执行环境偏好。

**目标：** 将普通攻击和受伤处理从硬编码重构为 effect 系统，支持特殊随从自定义攻击/受伤行为

**架构：** 攻击/受伤作为 effect（`onAttack`、`onTakeDamage`），通过事件传递（`GameEvent`）协调，effect 返回 `EffectResult`

**技术栈：** TypeScript, Zod, Vitest

**设计文档：** `docs/design/2026-03-12-攻击受伤Effect化-design.md`

---

## 任务 1：添加 GameEvent 类型定义

**文件：**
- 修改：`packages/shared/src/types/follower.ts`
- 修改：`packages/shared/src/types/index.ts`

**步骤 1：在 follower.ts 添加事件类型**

在 `EffectGameEvent` 接口后添加：

```typescript
// ===== 游戏事件 =====

export type DamageType = 'physical' | 'magical' | 'true'

// 伤害事件
export interface DamageEvent {
  type: 'damage'
  targets: FollowerInstance[]
  value: number
  damageType: DamageType
  source?: FollowerInstance
}

// 治疗事件
export interface HealEvent {
  type: 'heal'
  targets: FollowerInstance[]
  value: number
}

// 召唤事件
export interface SummonEvent {
  type: 'summon'
  followers: FollowerInstance[]
  positions: BattlePosition[]
}

// 死亡事件
export interface DeathEvent {
  type: 'death'
  follower: FollowerInstance
}

// 游戏事件联合类型
export type GameEvent = DamageEvent | HealEvent | SummonEvent | DeathEvent

// 效果返回值
export interface EffectResult {
  gameState: unknown
  events?: GameEvent[]
}
```

**步骤 2：更新 EffectFunction 返回值类型**

将：
```typescript
export type EffectFunction = (ctx: EffectContext) => unknown
```

改为：
```typescript
export type EffectFunction = (ctx: EffectContext) => EffectResult
```

**步骤 3：更新 TriggerTiming 添加新触发时机**

将：
```typescript
export type TriggerTiming =
  | 'onEnter'       // 入场
  | 'onGrowth'      // 成长
  | 'onFirstStrike' // 先手
  | 'onAttack'      // 攻击后
  | 'onHit'         // 受击
  | 'onKill'        // 击杀
  | 'onDeath'       // 死亡（遗言）
  | 'onRoundEnd'    // 回合结束（准备）
```

改为：
```typescript
export type TriggerTiming =
  | 'onEnter'        // 入场
  | 'onGrowth'       // 成长
  | 'onFirstStrike'  // 先手
  | 'onAttack'       // 攻击时
  | 'onTakeDamage'   // 受伤时
  | 'onHit'          // 受伤后
  | 'onKill'         // 击杀
  | 'onDeath'        // 死亡（遗言）
  | 'onRoundEnd'     // 回合结束（准备）
```

**步骤 4：更新 triggerTimingSchema**

将：
```typescript
export const triggerTimingSchema = z.enum([
  'onEnter', 'onGrowth', 'onFirstStrike', 'onAttack',
  'onHit', 'onKill', 'onDeath', 'onRoundEnd'
])
```

改为：
```typescript
export const triggerTimingSchema = z.enum([
  'onEnter', 'onGrowth', 'onFirstStrike', 'onAttack', 'onTakeDamage',
  'onHit', 'onKill', 'onDeath', 'onRoundEnd'
])
```

**步骤 5：更新类型导出**

在文件末尾确保导出：
```typescript
export type {
  // ... 现有导出
  DamageType,
  DamageEvent,
  HealEvent,
  SummonEvent,
  DeathEvent,
  GameEvent,
  EffectResult
}
```

**步骤 6：构建验证**

运行：`cd packages/shared && bun run build`
预期：构建成功，无类型错误

**步骤 7：提交**

```bash
git add packages/shared/src/types/follower.ts
git commit -m "feat(types): 添加 GameEvent 类型和 EffectResult"
```

---

## 任务 2：添加默认效果函数

**文件：**
- 创建：`packages/shared/src/utils/effects.ts`
- 修改：`packages/shared/src/utils/index.ts`

**步骤 1：创建 effects.ts**

```typescript
// packages/shared/src/utils/effects.ts
import type {
  EffectContext,
  EffectResult,
  DamageEvent,
  FollowerInstance,
  BattlePosition
} from '../types/follower'
import { getCurrentStats } from './follower'

/**
 * 默认攻击效果
 * 对随机敌人造成等同于攻击力的物理伤害
 */
export function defaultOnAttack(ctx: EffectContext): EffectResult {
  const stats = getCurrentStats(ctx.self)
  const target = ctx.tools.getRandomEnemy(ctx.gameState, ctx.self.position)
  
  if (!target) {
    return { gameState: ctx.gameState }
  }
  
  return {
    gameState: ctx.gameState,
    events: [{
      type: 'damage',
      targets: [target],
      value: stats.attack,
      damageType: 'physical',
      source: ctx.self
    }]
  }
}

/**
 * 默认受伤效果
 * 处理护盾消耗和扣血
 */
export function defaultOnTakeDamage(ctx: EffectContext): EffectResult {
  const newState = ctx.tools.cloneDeep(ctx.gameState)
  const damageEvent = ctx.event as DamageEvent | undefined
  
  if (!damageEvent) {
    return { gameState: newState }
  }
  
  // 在 newState 中找到自己
  const allies = (newState as { allies?: FollowerInstance[] }).allies
  const enemies = (newState as { enemies?: FollowerInstance[] }).enemies
  
  let self: FollowerInstance | undefined
  if (allies) {
    self = allies.find(f => f.instanceId === ctx.self.instanceId)
  }
  if (!self && enemies) {
    self = enemies.find(f => f.instanceId === ctx.self.instanceId)
  }
  
  if (!self) {
    return { gameState: newState }
  }
  
  // 护盾处理
  const shieldStatus = self.statusList.find(s => s.shield && s.shield > 0)
  if (shieldStatus) {
    shieldStatus.shield!--
    if (shieldStatus.shield === 0) {
      self.statusList = self.statusList.filter(s => s !== shieldStatus)
    }
    return { gameState: newState }
  }
  
  // 扣血
  self.currentHealth -= damageEvent.value
  
  return { gameState: newState }
}
```

**步骤 2：更新 index.ts 导出**

在 `packages/shared/src/utils/index.ts` 添加：
```typescript
export * from './effects'
```

**步骤 3：构建验证**

运行：`cd packages/shared && bun run build`
预期：构建成功

**步骤 4：提交**

```bash
git add packages/shared/src/utils/effects.ts packages/shared/src/utils/index.ts
git commit -m "feat(utils): 添加默认攻击/受伤效果函数"
```

---

## 任务 3：更新随从数据

**文件：**
- 修改：`packages/shared/src/data/followers.ts`

**步骤 1：查看现有随从数据**

运行：`head -100 packages/shared/src/data/followers.ts`

**步骤 2：导入默认效果**

在文件顶部添加导入：
```typescript
import { defaultOnAttack, defaultOnTakeDamage } from '../utils/effects'
```

**步骤 3：为普通随从添加 onAttack 引用**

对于没有特殊攻击效果的随从，添加 `onAttack` 引用默认效果：

```typescript
'F001': {
  id: 'F001',
  name: '小狼',
  description: '基础攻击型',
  level: 1,
  baseAttack: 2,
  baseHealth: 1,
  effects: {
    onAttack: defaultOnAttack  // 添加默认攻击
  },
  equipmentSlots: 2,
  imageUrl: '/assets/followers/f001.png'
},
```

**步骤 4：更新所有普通随从**

对所有现有随从添加 `onAttack: defaultOnAttack`

**步骤 5：构建验证**

运行：`cd packages/shared && bun run build`
预期：构建成功

**步骤 6：提交**

```bash
git add packages/shared/src/data/followers.ts
git commit -m "refactor(data): 为随从添加默认攻击效果引用"
```

---

## 任务 4：添加特殊随从示例（不死之王）

**文件：**
- 修改：`packages/shared/src/data/followers.ts`

**步骤 1：在 6 星随从区域添加不死之王**

```typescript
// ===== 6星随从 =====

'F601': {
  id: 'F601',
  name: '不死之王',
  description: '攻击时不造成伤害，而是召唤一个亡灵骷髅',
  level: 6,
  baseAttack: 4,
  baseHealth: 6,
  effects: {
    onAttack: (ctx: EffectContext): EffectResult => {
      const newState = ctx.tools.cloneDeep(ctx.gameState)
      
      // 找到空位置
      const allies = (newState as { allies?: FollowerInstance[] }).allies || []
      const occupiedPositions = new Set(allies.map(f => `${f.position.x},${f.position.y}`))
      let emptyPosition: BattlePosition | null = null
      
      for (let x = 0; x < 3; x++) {
        for (let y = 0; y < 2; y++) {
          if (!occupiedPositions.has(`${x},${y}`)) {
            emptyPosition = { side: 'ally', x: x as 0 | 1 | 2, y: y as 0 | 1 }
            break
          }
        }
        if (emptyPosition) break
      }
      
      if (!emptyPosition) {
        return { gameState: ctx.gameState }
      }
      
      const skeleton: FollowerInstance = {
        instanceId: `skeleton-${Date.now()}-${Math.random().toString(36).slice(2)}`,
        id: 'F_SKELETON',
        name: '亡灵骷髅',
        description: '',
        level: 1,
        baseAttack: 2,
        baseHealth: 1,
        ownerId: ctx.self.ownerId,
        position: emptyPosition,
        currentHealth: 1,
        statusList: [],
        equipment: [],
        equipmentSlots: 1,
        imageUrl: '/assets/followers/skeleton.png'
      }
      
      return {
        gameState: newState,
        events: [{
          type: 'summon',
          followers: [skeleton],
          positions: [emptyPosition]
        }]
      }
    }
  },
  equipmentSlots: 2,
  imageUrl: '/assets/followers/undead_king.png'
}
```

**步骤 2：添加导入类型**

确保导入类型：
```typescript
import type { EffectContext, EffectResult, FollowerInstance, BattlePosition } from '../types/follower'
```

**步骤 3：构建验证**

运行：`cd packages/shared && bun run build`
预期：构建成功

**步骤 4：提交**

```bash
git add packages/shared/src/data/followers.ts
git commit -m "feat(data): 添加特殊随从 - 不死之王"
```

---

## 任务 5：添加单元测试

**文件：**
- 创建：`packages/shared/src/utils/effects.test.ts`

**步骤 1：创建测试文件**

```typescript
// packages/shared/src/utils/effects.test.ts
import { describe, it, expect } from 'vitest'
import { defaultOnAttack, defaultOnTakeDamage } from './effects'
import type { EffectContext, FollowerInstance, BattlePosition, DamageEvent } from '../types/follower'

describe('defaultOnAttack', () => {
  it('应对随机敌人造成等同于攻击力的伤害', () => {
    const attacker: FollowerInstance = {
      instanceId: 'attacker-1',
      id: 'F001',
      name: '小狼',
      description: '',
      level: 1,
      baseAttack: 5,
      baseHealth: 3,
      ownerId: 'player-a',
      position: { side: 'ally', x: 0, y: 0 },
      currentHealth: 3,
      statusList: [],
      equipment: [],
      equipmentSlots: 2,
      imageUrl: ''
    }
    
    const target: FollowerInstance = {
      instanceId: 'target-1',
      id: 'F002',
      name: '敌人',
      description: '',
      level: 1,
      baseAttack: 2,
      baseHealth: 5,
      ownerId: 'player-b',
      position: { side: 'enemy', x: 0, y: 0 },
      currentHealth: 5,
      statusList: [],
      equipment: [],
      equipmentSlots: 2,
      imageUrl: ''
    }
    
    const gameState = {
      allies: [attacker],
      enemies: [target]
    }
    
    const ctx: EffectContext = {
      gameState,
      self: attacker,
      tools: {
        cloneDeep: <T>(obj: T) => JSON.parse(JSON.stringify(obj)),
        getRandomEnemy: () => target,
        getRandomAlly: () => null,
        getAllAllies: () => [attacker],
        getAllEnemies: () => [target],
        getColumnAllies: () => []
      }
    }
    
    const result = defaultOnAttack(ctx)
    
    expect(result.events).toBeDefined()
    expect(result.events!.length).toBe(1)
    expect(result.events![0].type).toBe('damage')
    
    const damageEvent = result.events![0] as DamageEvent
    expect(damageEvent.targets.length).toBe(1)
    expect(damageEvent.value).toBe(5)
    expect(damageEvent.damageType).toBe('physical')
    expect(damageEvent.source?.instanceId).toBe('attacker-1')
  })
  
  it('无敌人时应返回空事件', () => {
    const attacker: FollowerInstance = {
      instanceId: 'attacker-1',
      id: 'F001',
      name: '小狼',
      description: '',
      level: 1,
      baseAttack: 5,
      baseHealth: 3,
      ownerId: 'player-a',
      position: { side: 'ally', x: 0, y: 0 },
      currentHealth: 3,
      statusList: [],
      equipment: [],
      equipmentSlots: 2,
      imageUrl: ''
    }
    
    const gameState = {
      allies: [attacker],
      enemies: []
    }
    
    const ctx: EffectContext = {
      gameState,
      self: attacker,
      tools: {
        cloneDeep: <T>(obj: T) => JSON.parse(JSON.stringify(obj)),
        getRandomEnemy: () => null,
        getRandomAlly: () => null,
        getAllAllies: () => [attacker],
        getAllEnemies: () => [],
        getColumnAllies: () => []
      }
    }
    
    const result = defaultOnAttack(ctx)
    
    expect(result.events).toBeUndefined()
  })
})

describe('defaultOnTakeDamage', () => {
  it('应扣除目标当前血量', () => {
    const target: FollowerInstance = {
      instanceId: 'target-1',
      id: 'F002',
      name: '敌人',
      description: '',
      level: 1,
      baseAttack: 2,
      baseHealth: 5,
      ownerId: 'player-a',
      position: { side: 'ally', x: 0, y: 0 },
      currentHealth: 5,
      statusList: [],
      equipment: [],
      equipmentSlots: 2,
      imageUrl: ''
    }
    
    const gameState = {
      allies: [target],
      enemies: []
    }
    
    const damageEvent: DamageEvent = {
      type: 'damage',
      targets: [target],
      value: 3,
      damageType: 'physical'
    }
    
    const ctx: EffectContext = {
      gameState,
      self: target,
      tools: {
        cloneDeep: <T>(obj: T) => JSON.parse(JSON.stringify(obj)),
        getRandomEnemy: () => null,
        getRandomAlly: () => null,
        getAllAllies: () => [target],
        getAllEnemies: () => [],
        getColumnAllies: () => []
      },
      event: damageEvent
    }
    
    const result = defaultOnTakeDamage(ctx)
    
    const newAllies = (result.gameState as { allies: FollowerInstance[] }).allies
    expect(newAllies[0].currentHealth).toBe(2)
  })
  
  it('有护盾时应消耗护盾而不扣血', () => {
    const target: FollowerInstance = {
      instanceId: 'target-1',
      id: 'F002',
      name: '持盾随从',
      description: '',
      level: 1,
      baseAttack: 2,
      baseHealth: 5,
      ownerId: 'player-a',
      position: { side: 'ally', x: 0, y: 0 },
      currentHealth: 5,
      statusList: [{ shield: 1, permanent: true, source: '入场护盾' }],
      equipment: [],
      equipmentSlots: 2,
      imageUrl: ''
    }
    
    const gameState = {
      allies: [target],
      enemies: []
    }
    
    const damageEvent: DamageEvent = {
      type: 'damage',
      targets: [target],
      value: 3,
      damageType: 'physical'
    }
    
    const ctx: EffectContext = {
      gameState,
      self: target,
      tools: {
        cloneDeep: <T>(obj: T) => JSON.parse(JSON.stringify(obj)),
        getRandomEnemy: () => null,
        getRandomAlly: () => null,
        getAllAllies: () => [target],
        getAllEnemies: () => [],
        getColumnAllies: () => []
      },
      event: damageEvent
    }
    
    const result = defaultOnTakeDamage(ctx)
    
    const newAllies = (result.gameState as { allies: FollowerInstance[] }).allies
    expect(newAllies[0].currentHealth).toBe(5) // 血量不变
    expect(newAllies[0].statusList.length).toBe(0) // 护盾消耗
  })
})
```

**步骤 2：运行测试**

运行：`cd packages/shared && bun test src/utils/effects.test.ts`
预期：测试通过

**步骤 3：提交**

```bash
git add packages/shared/src/utils/effects.test.ts
git commit -m "test: 添加默认攻击/受伤效果单元测试"
```

---

## 任务 6：最终构建验证

**步骤 1：构建 shared 包**

运行：`cd packages/shared && bun run build`
预期：构建成功

**步骤 2：运行所有测试**

运行：`cd packages/shared && bun test`
预期：测试通过

**步骤 3：最终提交**

```bash
git add .
git commit -m "feat: 攻击/受伤 Effect 化实现完成"
```

---

## 文件变更摘要

| 文件 | 操作 |
|------|------|
| `packages/shared/src/types/follower.ts` | 修改 - 添加 GameEvent、EffectResult 类型 |
| `packages/shared/src/utils/effects.ts` | 创建 - 默认攻击/受伤效果 |
| `packages/shared/src/utils/index.ts` | 修改 - 导出 effects |
| `packages/shared/src/data/followers.ts` | 修改 - 添加 onAttack 引用和特殊随从 |
| `packages/shared/src/utils/effects.test.ts` | 创建 - 单元测试 |

---

## 注意事项

1. **EffectResult.gameState 类型**：当前为 `unknown`，实际使用时需要类型断言或定义更精确的 GameState 类型

2. **服务端战斗系统**：本计划只完成了类型和默认效果，战斗系统核心逻辑（`executeBattle`、`processEvent`）需要在 `packages/server` 中实现

3. **前端动画渲染**：服务端执行逻辑后如何触发前端动画，待后续讨论设计
